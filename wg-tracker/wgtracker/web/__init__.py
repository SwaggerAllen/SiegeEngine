"""Basic authenticated web UI (addendum to the spec).

First person to register becomes the admin. The admin can mint invite links that
grant read-only ("viewer") access. Everyone can browse the structured archive;
only admins can manage invites.
"""
